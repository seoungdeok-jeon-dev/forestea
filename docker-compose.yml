services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: forestea
      POSTGRES_PASSWORD: forestea
      POSTGRES_DB: forestea
    ports:
      - "5432:5432"
    volumes:
      - forestea_pg:/var/lib/postgresql/data

volumes:
  forestea_pg:
